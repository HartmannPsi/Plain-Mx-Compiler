package IR.IRNodes;

public class IRNLNode extends IRNode {
    // This is just for formatting, no practical effect

    public void printToString() {
        System.out.println();
        printNext();
    }
}
